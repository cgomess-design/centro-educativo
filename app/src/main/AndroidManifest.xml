<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/sgau"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sgau_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NexusApp">
        <activity android:name=".activities.FormEstudianteActivity" />
        <activity android:name=".activities.DetalleEstudianteActivity" />
        <activity android:name=".activities.EstudiantesActivity" />
        <activity android:name=".activities.DocenteActivity" />
        <activity android:name=".activities.DetalleDocenteActivity" />
        <activity android:name=".activities.FormDocenteActivity" />
        <activity android:name=".activities.NotasActivity" />
        <activity android:name=".activities.DetalleNotaActivity" />
        <activity android:name=".activities.FormNotaActivity" />
        <activity android:name=".activities.CursosActivity" />
        <activity android:name=".activities.FormCursoActivity" />
        <activity android:name=".activities.DetalleCursoActivity" />

        <activity android:name=".activities.InscripcionesActivity" />
        <activity android:name=".activities.DetalleInscripcionActivity" />
        <activity android:name=".activities.FormInscripcionActivity" />

        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
